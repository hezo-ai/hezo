/**
 * The inset shortcut keycap, which now ships from `@hezo/ui`.
 *
 * Re-exported from its old path so every call site keeps the import it had.
 */
export { kbdSizeClass, ShortcutKbd } from '@hezo/ui';
