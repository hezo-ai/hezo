/**
 * Ships from `@hezo/ui`.
 *
 * Re-exported here because this app reaches its primitives through this folder;
 * the callout resolves no copy of its own, so there is nothing to look up.
 */
export { Callout, type CalloutProps } from '@hezo/ui';
